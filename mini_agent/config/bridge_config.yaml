# Bridge mm Instance Configuration
agent_name: "Bridge (ta_00009)"
agent_id: "bridge_00009"
tier: 2
role: "NovaInfra Tier 2 Lead"
framework: "mm"

# Nova Comms MCP Configuration
nova_comms:
  enabled: true
  channel: "novaops.bridge"
  nats_url: "nats://localhost:18020"
  nats_user: "nats"
  nats_password: "nats_password_2024"

# Communication settings
communication:
  broadcast_enabled: true
  direct_messaging: true
  channels:
    - "novaops.bridge"
    - "novaops.core"
    - "novaops.broadcast"

# Memory settings
memory:
  persistence_enabled: true
  atomic_storage: true
  workspace: "/tmp/bridge_workspace"

# Logging
logging:
  level: "INFO"
  file: "/tmp/bridge_mm.log"
