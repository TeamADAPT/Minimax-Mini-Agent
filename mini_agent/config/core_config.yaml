# Core mm Instance Configuration
agent_name: "Core (ta_00008)"
agent_id: "core_00008"
tier: 1
role: "NovaOps Tier 1 Lead"
framework: "mm"

# Nova Comms MCP Configuration
nova_comms:
  enabled: true
  channel: "novaops.core"
  nats_url: "nats://localhost:18020"
  nats_user: "nats"
  nats_password: "nats_password_2024"

# Communication settings
communication:
  broadcast_enabled: true
  direct_messaging: true
  channels:
    - "novaops.core"
    - "novaops.bridge"
    - "novaops.broadcast"

# Memory settings
memory:
  persistence_enabled: true
  atomic_storage: true
  workspace: "/tmp/core_workspace"

# Logging
logging:
  level: "INFO"
  file: "/tmp/core_mm.log"
