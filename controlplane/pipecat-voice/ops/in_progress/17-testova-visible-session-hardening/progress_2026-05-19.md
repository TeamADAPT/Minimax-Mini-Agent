# Testova Visible Session Progress

## 2026-05-19 20:34:19 — SIGNED_BY_AGENT

Current state:

- `Testova CLI` was relaunched as a fresh visible Hermes session from `/adapt/novas/active/Testova`.
- `testova-tui-nats-bridge.service` is running against the `Testova CLI` window.
- `/adapt/novas/active/Testova/.env` and `/home/x/.hermes/profiles/testova/.env` were corrected to valid `python-dotenv` syntax by removing shell `if/source/fi` control flow.
- The Testova bridge user unit was changed to load `/adapt/secrets/m2.env`, disable CLI fallback, use a 90 second visible delivery window, and wait 300 seconds for visible assistant reply capture.
- A matching tracked unit spec was added at `systemd/testova-tui-nats-bridge.service`.
- A held proof run was stopped after Chase said `hold`.
- After the held proof left the bridge waiting on a closed window, the bridge was restarted and `Testova CLI` was reopened idle with no new NATS prompt.

Evidence observed:

- Runtime cwd for the visible Testova Hermes process resolved to `/adapt/novas/active/Testova`.
- `testova-tui-nats-bridge.service` restarted cleanly after the unit change.
- Testova produced an assistant message in the visible CLI session after the env cleanup, proving the model/session path can answer again.
- Final idle state after hold cleanup: visible `Testova CLI` window present, Hermes process running, cwd `/adapt/novas/active/Testova`, bridge active since `2026-05-19 20:35:37`.
- GNOME reset the visible window title to `Terminal`; retitling window `77651457` back to `Testova CLI` restored bridge title discovery.

Remaining acceptance gate:

- Task 17 is not complete until three consecutive `nova.testova.direct` prompts land in the intended visible CLI session and return reply payloads with no `fallback` or `error`.
- Testova remains validation-only until that proof passes.

## 2026-05-19 20:42:11 — SIGNED_BY_AGENT

Window targeting hardening:

- Added optional `ECHO_TUI_WINDOW_CLASS` and `ECHO_TUI_WINDOW_ID` targeting to `scripts/echo_tui_nats_bridge.py`.
- Updated `systemd/testova-tui-nats-bridge.service` and the live user unit with `ECHO_TUI_WINDOW_CLASS=TestovaCLI`.
- Relaunched Testova in a GNOME Terminal window using class `TestovaCLI`.
- Restarted `testova-tui-nats-bridge.service` after `systemctl --user daemon-reload`.

Evidence observed:

- `python3 -m py_compile scripts/echo_tui_nats_bridge.py` passed.
- `xdotool search --class TestovaCLI` found the visible Testova terminal.
- The Testova Hermes process is running under `/home/x/.local/bin/hermes -p testova --yolo`.
- `testova-tui-nats-bridge.service` is active and ready for `nova.testova.direct/.meet/.ping`.

Remaining acceptance gate:

- No new NATS proof prompt was sent because Chase requested hold.
- Task 17 remains in progress until the three-turn visible-only proof passes.
