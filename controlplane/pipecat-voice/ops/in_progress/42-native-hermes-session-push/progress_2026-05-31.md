# Native Hermes Session Push Progress

## 2026-05-31 08:39:08 - SIGNED_BY_AGENT

## Work Completed

- Inspected Hermes API server session endpoints.
- Confirmed the API server advertises:
  - `POST /api/sessions`
  - `POST /api/sessions/{session_id}/chat`
  - `POST /api/sessions/{session_id}/chat/stream`
  - `GET /api/sessions/{session_id}/messages`
- Found and patched a Hermes API-server crash where `AIAgent` received `model`
  twice when `_resolve_runtime_agent_kwargs()` already returned a model.
- Local Hermes commit:
  `1ae80fb1c Fix API server duplicate model argument - SIGNED_BY_AGENT`
- Push to upstream Hermes failed with HTTP 403 because the configured account
  does not have write access to `NousResearch/hermes-agent`.
- Restarted `hermes-gateway-echo.service` to load the patch.
- Restarted `hermes-gateway-vox.service` and `hermes-gateway-zap.service` for
  bounded API-session proofs.
- Patched the Rust bridge so it attempts Hermes API session delivery before
  subprocess fallback:
  - Reads API port from profile config.
  - Reads API key from profile config or `API_SERVER_KEY`.
  - Creates or reuses a deterministic `nats_<agent>_<sender>` session.
  - Calls `/api/sessions/{session_id}/chat`.
  - Falls back to subprocess when API session delivery fails or returns empty.

## Proof Results

Echo:

- API capabilities endpoint worked with the fleet API key.
- `POST /api/sessions` worked.
- Before patch: `/api/sessions/{id}/chat/stream` crashed with duplicate `model`
  argument.
- After patch: duplicate-model crash cleared, but Echo entered provider retry
  because the profile currently resolves `grok-4.3` through an invalid fallback
  provider path.
- Echo was force-killed and restarted to stop the provider retry loop.

Iris and Tecton:

- API session endpoints were reachable.
- Session chat wrote the user turn to `state.db`.
- Provider calls failed because xAI returned account/credit authorization
  failures.

Vox:

- API session endpoints were reachable.
- Session chat wrote the user turn to `state.db`.
- Provider call failed because the configured Nous model name was not found
  upstream.

Zap:

- API session endpoints were reachable.
- Session chat wrote the user turn to `state.db`.
- Provider call failed because DeepSeek returned insufficient balance.

## Current Gate

Native API session push is structurally available, but not promotable yet.

The blocker is no longer NATS reachability or missing Hermes API endpoints. The
current blockers are:

- Provider configuration/credit failures across the tested profiles.
- Need one successful assistant-row persistence proof after provider repair.
- Need streaming reply chunk proof before replacing visible-terminal injection.

## Verification

- Hermes API patch syntax check:
  `python3 -m py_compile gateway/platforms/api_server.py`
- Rust bridge:
  - `cargo fmt`
  - `cargo fmt --check`
  - `cargo check`
  - `cargo build --release`

## Next Step

Repair or select one known-good profile/provider, then rerun Task 42 proof:

1. `POST /api/sessions`
2. `POST /api/sessions/{session_id}/chat/stream`
3. Verify streamed assistant content.
4. Verify `state.db` contains both user and assistant rows.
5. Restart/promote the Rust bridge only after the proof passes.
