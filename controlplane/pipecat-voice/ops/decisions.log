# Decisions Log

## 2026-05-31 10:13:14 — SIGNED_BY_AGENT
Decision: Start the Rust voice provider abstraction as a contract/config slice before replacing the live Deepgram path.
Reason: The current gateway path is working and already protects long-lived Deepgram keys; Rust ownership should first harden provider selection, redaction, and experimental xAI gating without destabilizing the browser voice loop.

## 2026-05-31 10:11:43 — SIGNED_BY_AGENT
Decision: Complete Task 44 with local canonical JSONL plus API handoff, leaving Temporal and RedPanda as downstream consumers rather than blocking the gateway on them.
Reason: The gateway must not block hot voice turns on workflow or streaming infrastructure; a scoped append-only file gives memory and analytics an immediate durable contract while Temporal/RedPanda can tail, replay, and enrich `comms.turn.v1` events independently.

## 2026-05-31 10:07:29 — SIGNED_BY_AGENT
Decision: Grant `pipecat-voice.service` write access to `/adapt/novas/active/_shared` instead of moving canonical turn events under repo runtime.
Reason: The shared nova path is the right memory handoff boundary; keeping events only under repo-local runtime would make memory consumers scrape or copy a less canonical source.

## 2026-05-31 10:01:52 — SIGNED_BY_AGENT
Decision: Start transcript-memory work immediately after observability and make canonical event emission the next promotion surface.
Reason: The activity page now exposes derived session and room metrics, but memory and analytics should consume normalized turn events instead of scraping UI text, room-history rows, or Hermes session tables directly.

## 2026-05-31 09:59:24 — SIGNED_BY_AGENT
Decision: Ignore browser verification screenshots, local Playwright state, and gateway runtime logs instead of committing them.
Reason: They are generated operational artifacts and do not belong in version control; the task completion report records their verification role.

## 2026-05-31 09:57:08 — SIGNED_BY_AGENT
Decision: Complete Task 45 with derived live observability instead of adding a new metrics database.
Reason: Existing route state, NATS monitor snapshots, Hermes profile `state.db` files, room history, session events, and ops task directories already answer the operator's first-order health questions; Task 44 should own the canonical transcript-memory persistence path before analytics storage is expanded.

## 2026-05-31 09:51:11 — SIGNED_BY_AGENT
Decision: Implement CommsOps observability from existing live surfaces before adding new storage.
Reason: NATS monitor state, route metadata in room history, profile `state.db` files, systemd health, and task directories already provide enough high-value reporting; using them first gives immediate operator visibility while Task 44 defines canonical transcript ingestion.

## 2026-05-31 09:35:13 — SIGNED_BY_AGENT
Decision: Route repaired Iris, Echo, and Tecton `auto` phone/browser turns to the Rust runtime by default.
Reason: Task 42 and Task 47 proved the Rust API-session path, provider health, streamed chunks, route metadata, and session persistence for these agents; keeping auto on older routes would preserve avoidable TUI/daemon fragility.

## 2026-05-31 09:24:30 — SIGNED_BY_AGENT
Decision: Start phone/browser route cutover after transport proof, not before.
Reason: The UI should change only after the Rust route has proof IDs, route metadata, denoised chunks, and rollback instructions; this keeps operator-facing behavior tied to verified runtime health.

## 2026-05-31 09:22:58 — SIGNED_BY_AGENT
Decision: Close Task 42 at runtime-scoped Rust route promotion and split phone/browser `auto` cutover into Task 48.
Reason: The transport/session path is now proven, but changing the default UI route is a separate operator-facing behavior change that needs its own rollback and proof surface.

## 2026-05-31 09:22:19 — SIGNED_BY_AGENT
Decision: Keep full Hermes assistant messages in session persistence and denoise only the Rust voice stream.
Reason: Audit/state should preserve the agent's actual response, while phone and voice route consumers need concise spoken chunks without identity signatures, CWD lines, or date tails.

## 2026-05-31 09:13:10 — SIGNED_BY_AGENT
Decision: Repair user-facing provider routes incrementally, starting with Iris, Echo, and Tecton.
Reason: The Rust transport path is proven; remaining failures are profile-specific provider/account/model problems, so single-profile repair with backups and proofs reduces blast radius while moving the original Task 42 dependency set toward promotion.

## 2026-05-31 09:11:23 — SIGNED_BY_AGENT
Decision: Promote the Rust API-session path only for `commscanary` and create Task 47 for user-facing provider repair.
Reason: The native bridge path is now proven with streamed chunks, route metadata, and session persistence, but user-facing profiles still fail on provider/account/model state; separating provider repair prevents a transport win from being hidden behind unrelated upstream failures.

## 2026-05-31 09:01:31 — SIGNED_BY_AGENT
Decision: Use NVIDIA NIM `qwen/qwen3.5-397b-a17b` as the CommsOps canary lane instead of Groq.
Reason: Groq passed direct HTTP canary quickly, but the standard Hermes agent payload exceeded that route's request limits; NVIDIA Qwen passed direct provider proof, Hermes CLI proof, API sync proof, API stream proof, and session persistence proof with a large-context lane.

## 2026-05-31 08:47:58 — SIGNED_BY_AGENT
Decision: Probe direct provider APIs before changing more Hermes profile configs.
Reason: Direct one-token canaries isolate account/model health from Hermes cold-start, memory, tool, and gateway behavior, reducing quota burn and avoiding unnecessary service churn.

## 2026-05-31 08:44:16 — SIGNED_BY_AGENT
Decision: Split provider/model repair into `46-commsops-provider-health-gate` instead of continuing Task 42 proof attempts across random agents.
Reason: Repeated proof attempts were consuming time and provider quota while failing for unrelated account/model reasons; Task 42 should stay focused on transport/session semantics, and provider health needs its own canary acceptance gate.

## 2026-05-31 08:39:08 — SIGNED_BY_AGENT
Decision: Do not promote the Rust API-session path to live subject ownership yet.
Reason: Hermes API session endpoints are available and the Rust bridge can now call them, but all tested provider-backed proofs failed before assistant persistence; promotion requires a successful streamed assistant response and both user/assistant rows in `state.db`.

## 2026-05-31 08:32:00 — SIGNED_BY_AGENT
Decision: Force-kill and restart `hermes-gateway-echo.service` after a bounded API-session proof exposed a long provider retry loop.
Reason: The proof had already exceeded the client timeout and the gateway continued retrying an invalid provider/model route; force restart stopped wasted provider retries and restored the Echo gateway to active service.

## 2026-05-31 08:20:40 — SIGNED_BY_AGENT
Decision: Start native Hermes session push with inspection and bounded proofs instead of immediately switching route ownership.
Reason: The active voice path is working and should not be destabilized; session attachment behavior must be measured across Hermes API, native adapter, and existing bridge paths before CommsOps can retire visible-terminal injection.

## 2026-05-31 08:14:38 — SIGNED_BY_AGENT
Decision: Make native Hermes session push, Rust voice provider abstraction, transcript memory pipeline, and CommsOps observability separate follow-up tasks instead of one umbrella rebuild.
Reason: Each lane has different proof gates, rollback mechanics, and owners; splitting them preserves forward motion while preventing voice-provider experiments or dashboard work from blocking the core route-promotion path.

## 2026-05-31 08:13:16 — SIGNED_BY_AGENT
Decision: Accept CommsOps ownership as a formal operating boundary and record it in repo docs before changing live services.
Reason: Chase granted T1 authority for the communication domain; durable authority, owned surfaces, and promotion gates reduce recurring ambiguity and give future agents a concrete control surface for voice, NATS, Hermes sessions, transcripts, and observability.

## 2026-05-19 22:54:10 — SIGNED_BY_AGENT
Decision: Start Task 34 as a no-prompt resume-gate preparation task while leaving Task 25 locked.
Reason: Testova remains under operator hold, so the only valid Testova work is state inspection and a documented pass/fail gate; Task 25 cannot be accepted until Testova is explicitly released and returns usable validation outputs.

## 2026-05-19 22:52:13 — SIGNED_BY_AGENT
Decision: Count Task 22 acceptance against the `vox` voice route, not Echo or Skipper.
Reason: The objective is end-to-end voice validation through the active gateway/STT/NATS/reply/TTS path; Echo and Skipper are currently reply-capture degraded for work prompts, while Vox produced three complete voice turns without changing live direct subject ownership or touching held Testova proof work.

## 2026-05-19 22:44:19 — SIGNED_BY_AGENT
Decision: Restart `pipecat-voice.service` during Task 22 validation.
Reason: The live service was active but stale relative to committed gateway code, producing a concrete `404` on `/api/session-state`; restarting the single gateway service loaded the current code without changing NATS subject ownership or held Testova state.

## 2026-05-19 22:42:59 — SIGNED_BY_AGENT
Decision: Start Task 22 with bounded diagnostics rather than forcing live audio if upstream or agent reply capture is degraded.
Reason: Task 22 acceptance requires three successful voice turns or named root causes for failures; current Echo/Skipper direct prompt capture is already degraded, so validation must measure each path and avoid destabilizing the gateway.

## 2026-05-19 22:40:41 — SIGNED_BY_AGENT
Decision: Treat Echo and Skipper work-prompt timeouts as route-capture blockers while continuing team work through Iris, Tecton, and Vox.
Reason: Ping health alone is not proof of usable team execution; Iris, Tecton, and Vox produced bounded replies, but Echo/Skipper did not return usable assignment responses and should not be represented as working until reply capture is recovered.

## 2026-05-19 22:39:57 — SIGNED_BY_AGENT
Decision: Implement Task 33 as a dashboard-only metrics slice backed by existing API and websocket surfaces.
Reason: The task requires actionable route/voice/NATS metrics without raw JSON hunting; existing `/api/session-state`, `/api/ops/status`, and `/ws/monitor` already expose enough real/derived data, so adding backend routes or changing live services would increase risk without improving the acceptance target.

## 2026-05-19 22:35:43 — SIGNED_BY_AGENT
Decision: Start Task 33 while Skipper and Echo process live NATS assignments.
Reason: The operator requested the whole team working; Task 33 can advance from existing dashboard/session-state/monitor surfaces while preserving the held Testova gate and current live direct subject owners.

## 2026-05-19 22:33:49 — SIGNED_BY_AGENT
Decision: Complete Task 32 with a read-only plus dry-run hook instead of enabling live pipecat-to-NATS publishing.
Reason: Task 22 has not yet validated end-to-end voice turns; the safest useful hook is an inspectable target/envelope contract that preserves default voice behavior and cannot publish unless a later explicit gate enables it.

## 2026-05-19 22:29:33 — SIGNED_BY_AGENT
Decision: Start Task 32 as a guarded, non-default pipecat session hook.
Reason: Voice validation is still pending under Task 22, so Task 32 must expose or dry-run route/session targets without changing the default voice path or stealing NATS subject ownership.

## 2026-05-19 22:28:32 — SIGNED_BY_AGENT
Decision: Keep Task 31 as a package plan artifact in NovaOps ops docs instead of modifying the Paperclip source tree.
Reason: The task acceptance is a concrete adapter package plan; Paperclip already contains refreshed Nova fleet notes, and the Paperclip repository has unrelated dirty ops-log work that should not be touched for this planning slice.

## 2026-05-19 22:25:51 — SIGNED_BY_AGENT
Decision: Start Task 31 instead of resuming Testova Task 17.
Reason: The operator hold on Testova proof work remains active; Paperclip adapter packaging is independent of Testova live prompts and continues the post-Task-30 Action Plan without disturbing current route owners.

## 2026-05-19 22:24:53 — SIGNED_BY_AGENT
Decision: Push the `/adapt/novas` Task 30 adapter artifact to `origin/task30-hermes-nats-adapter-promotion-gates`.
Reason: Local `/adapt/novas` `main` is not suitable for a direct main push because the remote main is non-fast-forward; a task branch preserves the verified adapter artifact without merging unrelated remote history.

## 2026-05-19 22:22:39 — SIGNED_BY_AGENT
Decision: Keep the Rust Hermes NATS adapter at no-go for live direct-subject ownership after Task 30.
Reason: The isolated proof passed correlation, reply, log, timeout, and secret-handling gates on `nova.adapter-proof.direct`, but live `nova.echo.direct` and `nova.skipper.direct` still require visible Hermes CLI turn delivery and final answer capture through their current TUI bridge owners.

## 2026-05-19 22:07:36 — SIGNED_BY_AGENT
Decision: Start Task 30 as an isolated-subject adapter promotion gate pass.
Reason: Echo and Skipper visible TUI bridges are the current live owners; adapter promotion must prove correlation, reply, logging, and rollback behavior without stealing live direct subjects.

## 2026-05-19 22:05:47 — SIGNED_BY_AGENT
Decision: Push the `/adapt/novas` bootstrap verifier commit to `origin/task29-nova-bootstrap-rust-verify` instead of pulling and merging remote `main`.
Reason: `origin/main` rejected the direct push as non-fast-forward; a task branch preserves the exact verified artifact without introducing unrelated remote merge risk during Task 29 completion.

## 2026-05-19 22:05:47 — SIGNED_BY_AGENT
Decision: Fix bootstrap generation defects discovered during canary verification instead of reporting a pass-only result.
Reason: The canary exposed two operationally material issues: dotenv-incompatible shell control flow in generated `.env` files and an incorrect Kimi context length.

## 2026-05-19 22:01:28 — SIGNED_BY_AGENT
Decision: Start Task 29 while leaving Task 17 in progress and held.
Reason: Testova proof work requires explicit operator release, but Rust bootstrap build verification is independent and advances the PEA/nova bootstrap queue without burning model quota or changing live nova sessions.

## 2026-05-19 22:00:23 — SIGNED_BY_AGENT
Decision: Relaunch Skipper fresh without `-c` while pinning `moonshotai/kimi-k2.6`.
Reason: Prior Echo/Skipper route proof showed continued sessions can visibly answer while weakening NATS reply capture, so the visible Skipper lane should use the fresh-session posture.

## 2026-05-19 21:31:30 — SIGNED_BY_AGENT
Decision: Gate Testova follow-up as Task 34 instead of folding it into the active PEA queue.
Reason: The operator hold on Testova prompts remains in force, so the queue should preserve the resume procedure without accidentally executing it.

## 2026-05-19 21:31:30 — SIGNED_BY_AGENT
Decision: Decompose PEA/bootstrap follow-up into new numbered task folders instead of expanding Task 28 itself.
Reason: Task 28 acceptance requires at least five executable tasks with disjoint owners and order; keeping implementation detail inside Task 28 would recreate an umbrella task.

## 2026-05-19 21:28:48 — SIGNED_BY_AGENT
Decision: Enforce decomposition against active queues only, while preserving historical completed tasks that predate the stricter template.
Reason: Task 27 acceptance concerns current and future executable work; rewriting older completed task files would create documentation churn without improving the active operating surface.

## 2026-05-19 21:28:48 — SIGNED_BY_AGENT
Decision: Treat the old Task 14 umbrella as closable audit debt under Task 27.
Reason: The concrete Task 14 slices have been completed as separate task folders and commits, so leaving the umbrella in `ops/in_progress` violates the one-ownable-slice rule.

## 2026-05-19 21:27:34 — SIGNED_BY_AGENT
Decision: Keep Paperclip fleet sync as an adapter-facing documentation refresh, not a live Paperclip data mutation.
Reason: The task acceptance requires Paperclip-facing artifacts to reflect real fleet posture; updating the redacted adapter notes avoids storing secrets or changing runtime state while making the current routes and constraints visible.

## 2026-05-19 21:26:20 — SIGNED_BY_AGENT
Decision: Refresh Paperclip from live route-state and recent task completions rather than copying historical fleet assumptions.
Reason: Paperclip must mirror current operating truth: Echo and Skipper are visible-ready after Task 24, Testova remains held/degraded, and Skipper orchestration uses the Task 23 contract.

## 2026-05-19 21:24:03 — SIGNED_BY_AGENT
Decision: Treat fresh visible sessions, not continued `-c` sessions, as the required launch mode for Echo execution-lane proof.
Reason: Echo visibly answered while continued from an old session, but the bridge could not correlate the turn to the NATS reply inbox; a fresh no-continue launch returned three consecutive replies cleanly.

## 2026-05-19 21:12:14 — SIGNED_BY_AGENT
Decision: Treat visible terminal restoration as the first gate for Task 24.
Reason: The execution lane requires a stable Echo route and Skipper-directed completions, but live checks showed both `Echo CLI` and `Skipper CLI` windows missing.

## 2026-05-19 21:10:47 — SIGNED_BY_AGENT
Decision: Complete Task 23 from the local contract artifact despite Skipper's reply timeout.
Reason: The acceptance criterion is a usable delegation contract, and Skipper's timeout is route/session evidence already captured in the contract caveat rather than missing contract content.

## 2026-05-19 21:09:29 — SIGNED_BY_AGENT
Decision: Record current visible-route degradation inside the Skipper contract instead of presenting the orchestration surface as fully stable.
Reason: Live route state reported Echo `visible-missing` and Skipper `fallback-active`, so the contract must distinguish assignment structure from visible-only proof readiness.

## 2026-05-19 21:06:39 — SIGNED_BY_AGENT
Decision: Build Task 23 as a Skipper-specific orchestration contract that references the existing team delegation protocol instead of replacing it.
Reason: `ops/team_delegation_protocol.md` already defines broad fleet safety and route rules; Skipper needs the narrower operational format for issuing owner-bound tasks with proof and return routes.

## 2026-05-19 21:04:10 — SIGNED_BY_AGENT
Decision: Make the monitor client classify and summarize live NATS events while retaining `nova.>` as the raw fallback preset.
Reason: Operators need fast proof, failure, route, ping, and log recognition during live debugging, but raw payload access must remain one click away for low-level inspection.

## 2026-05-19 21:03:20 — SIGNED_BY_AGENT
Decision: Remove the duplicate placeholder `/ws/monitor` route and let the NATS streaming handler be the only websocket owner.
Reason: Duplicate route registration can shadow the working stream and make the monitor appear connected without delivering live NATS traffic.

## 2026-05-19 20:57:20 — SIGNED_BY_AGENT
Decision: Keep monitor cleanup focused on the static `/monitor` client and existing `/ws/monitor` stream.
Reason: Task 21 can improve live NATS debugging without altering held Testova prompt state or widening the already-dirty gateway surface.

## 2026-05-19 20:56:40 — SIGNED_BY_AGENT
Decision: Treat class-based terminal discovery as equivalent to title discovery in route-state snapshots.
Reason: visible Hermes terminals can be intentionally addressed by stable GNOME class when the title is mutable, and the dashboard must reflect the bridge's actual targeting contract.

## 2026-05-19 20:55:18 — SIGNED_BY_AGENT
Decision: Keep the observability dashboard backed by `/api/session-state` and existing status endpoints instead of adding live model/NATS probes.
Reason: the dashboard must be safe for frequent polling and should show current route posture without burning model quota or disturbing held Testova proof work.

## 2026-05-19 20:49:20 — SIGNED_BY_AGENT
Decision: Expose session state through a separate read-only `/api/session-state` route instead of overloading `/api/ops/status`.
Reason: dashboard and monitor clients need frequent polling of agent route/session/proof state without triggering model calls, NATS request traffic, service restarts, journal scans, or writes.

## 2026-05-19 20:47:13 — SIGNED_BY_AGENT
Decision: Map current Codex OAuth auth into Testova's Hermes auth schema instead of copying the raw Codex auth file over it.
Reason: Hermes expects provider and credential-pool fields; raw-copying `/home/x/.codex/auth.json` would lose the active-provider structure Testova needs.

## 2026-05-19 20:45:02 — SIGNED_BY_AGENT
Decision: Remove the Testova bridge `ECHO_TUI_WINDOW_CLASS` override and rely on the explicit `Testova CLI` title.
Reason: the bridge checks class before title when class is set, and the GNOME Terminal window was discoverable by title while class lookup failed.

## 2026-05-19 20:42:11 — SIGNED_BY_AGENT
Decision: Prefer class/window-id targeting over title-only targeting for Testova visible bridge delivery.
Reason: GNOME Terminal can reset the displayed title to `Terminal` while the Hermes process remains alive, which makes title-only `xdotool search --name` unsuitable as the sole routing key.

## 2026-05-19 20:41:59 — SIGNED_BY_AGENT
Decision: Implement the session-state API as a read-only runtime-snapshot aggregation layer.
Reason: frequent dashboard polling should not trigger NATS requests, journal scans, model calls, or filesystem writes; the route should assemble existing snapshots into one safe response.

## 2026-05-19 20:40:43 — SIGNED_BY_AGENT
Decision: Classify route state from bridge service activity, visible window discovery, and fallback configuration, with proof metadata as evidence rather than the primary health switch.
Reason: a route can be currently ready or degraded independent of the last proof outcome; the snapshot needs both live posture and historical proof/reply context.

## 2026-05-19 20:38:10 — SIGNED_BY_AGENT
Decision: Preserve the existing `crew_route_state.json` fields while adding richer route posture fields.
Reason: downstream consumers already read `route_mode`, `fallback_enabled`, service, window, and session fields; Task 18 should improve fidelity without breaking existing UI/backend readers.

## 2026-05-19 20:34:19 — SIGNED_BY_AGENT
Decision: Keep Testova in Task 17 and validation-only until it passes three consecutive visible-only NATS proofs.
Reason: the env/model path now produces a visible assistant reply, but earlier attempts were polluted by stale queued fallback work and the current hold prevented completing the formal three-turn acceptance gate.

## 2026-05-19 20:34:18 — SIGNED_BY_AGENT
Decision: Treat `Testova CLI` window title discovery as part of the visible-session acceptance gate.
Reason: the Hermes process can remain alive in `/adapt/novas/active/Testova` while GNOME exposes the window as `Terminal`; the bridge will not deliver visible turns unless the expected title is discoverable.

## 2026-05-19 20:33:52 — SIGNED_BY_AGENT
Decision: Disable Testova's CLI fallback during visible-session hardening and increase reply capture to 300 seconds.
Reason: the fallback path masked whether `nova.testova.direct` reached the intended terminal, and the prior 30 second assistant-capture window was shorter than the model/provider path now configured for Testova.

## 2026-05-19 20:01:30 — SIGNED_BY_AGENT
Decision: Treat dedicated visible NATS terminals as fresh sessions, not resumed `--continue` sessions, when reliable reply capture matters.
Reason: the continued Skipper lineage carried prior tool activity and caused one proof turn to exceed the bridge reply window, while a fresh visible session cleared five consecutive direct-turn proofs without fallback.

## 2026-05-19 19:47:49 — SIGNED_BY_AGENT
Decision: Materialize the execution plan as task directories before resuming deeper implementation.
Reason: Skipper needs a concrete queue with ownership, acceptance, and rollback boundaries in order to orchestrate effectively; keeping the plan only in chat would recreate ambiguity at handoff time.

## 2026-05-19 10:48:16 — SIGNED_BY_AGENT
Decision: Spend overnight quota on telemetry and recovery loops instead of model-driven autonomous prompting.
Reason: the operator wants the crew moving while asleep, but current quota is constrained and the highest-leverage overnight work is to preserve route health, session posture, and pipecat visibility without burning turns on speculative agent chatter.

## 2026-05-19 05:47:38 — SIGNED_BY_AGENT
Decision: Treat visible TUI delivery and subject continuity as separate concerns by keeping Echo on strict visible-CLI reply capture, while enabling hidden CLI fallback only for Skipper and Testova when their visible terminals fail to persist turns within the bridge timeout.
Reason: Echo is currently stable on the visible route, but Skipper and Testova both exhibit intermittent GNOME Terminal/TUI persistence failures; a single-subject fallback inside the same bridge preserves collaboration without reintroducing overlapping NATS responders.

## 2026-05-19 04:50:11 — SIGNED_BY_AGENT
Decision: Apply the fleet model pass by rewriting Hermes config surfaces to `model.default: gpt-5.4`, `model.provider: openai-codex`, `model.base_url: https://chatgpt.com/backend-api/codex`, and `agent.reasoning_effort: medium`, while leaving existing key material untouched and only backfilling missing `auth.json` files from the already-working Codex auth source.
Reason: The operator asked for one shared `gpt-5.4` medium-thinking baseline without rotating credentials, and the only safe way to avoid stranding no-auth novas was to supply the same working Codex auth to profiles that lacked it.

## 2026-05-19 00:15:20 — SIGNED_BY_AGENT
Decision: Add `nova.latch.direct` as an inbox/logging service rather than pretending Codex is already a live NATS responder.
Reason: Echo needed a stable target for initiating messages to Latch; durable capture plus ACK is the correct first step until a true interactive responder is built.

## 2026-05-19 00:01:10 — SIGNED_BY_AGENT
Decision: Stop treating Skipper's latest missed turns as a bridge defect and use short direct prompts only after model/provider responsiveness is restored.
Reason: Chase confirmed the direct Return path was manual/operator-side and the remaining failure is provider latency; the visible bridge and NATS route already have earlier proof events.

## 2026-05-18 23:52:10 — SIGNED_BY_AGENT
Decision: Treat Skipper as transport-reachable but not currently reliable for fresh multi-turn task ownership until the visible bridge proves nonce-correct replies.
Reason: Echo returned a fresh second-turn task response, but Skipper produced one unrelated response and then timed out on a nonce-bound freshness probe.

## 2026-05-18 23:20:03 — SIGNED_BY_AGENT
Decision: Keep the PEA NATS transport as a small typed Rust crate and keep Hermes CLI turn execution in the existing control-plane bridge until the native bridge promotion gates pass.
Reason: this gives bootstrap and Paperclip work a reusable subject/envelope/request layer now while avoiding an unproven live ownership switch for visible CLI sessions.

## 2026-05-18 23:10:48 — SIGNED_BY_AGENT
Decision: Use a visible CLI-backed bridge for Skipper collaboration instead of the hidden profile-backed responder.
Reason: Chase needs collaboration prompts to land in Skipper's visible CLI, and the profile bridge can prove NATS transport without making the work observable in the terminal.

## 2026-05-18 22:43:18 — SIGNED_BY_AGENT
Decision: Keep the auth handoff as a locked local file and use NATS-visible collaboration proofs for Skipper and Echo rather than copying token material into memos or docs.
Reason: the team needs account handoff continuity and collaboration state, but secrets must remain out of tracked artifacts and chat transcripts.

## 2026-05-18 21:41:12 — SIGNED_BY_AGENT
Decision: Ship the fleet-control UI as a standalone static page under the existing gateway static mount.
Reason: this proves the useful operator surface without editing the already-modified gateway or existing client shell.

## 2026-05-18 21:34:03 — SIGNED_BY_AGENT
Decision: Add a tracked-file scanner instead of a destructive cleanup hook.
Reason: the branch scrub is complete, and the next guardrail should block reintroduction of common secret classes without printing values or mutating developer files.

## 2026-05-18 21:28:19 — SIGNED_BY_AGENT
Decision: Treat the approved next steps as one bounded hardening task with separate proofs for Skipper, Rust bridge, secret guardrail, Paperclip sync, and fleet-control UI.
Reason: the fleet route is now working enough to delegate, but promotion needs proof artifacts and guardrails before Echo ownership changes.

## 2026-05-18 21:12:26 — SIGNED_BY_AGENT
Decision: Add Skipper to the existing profile-backed Hermes NATS bridge instead of creating a separate subject owner.
Reason: Skipper has a valid Hermes profile and active directory, and `pipecat-hermes-agents.service` already owns the non-Echo profile-backed nova routes with one-owner semantics.

## 2026-05-18 21:08:39 — SIGNED_BY_AGENT
Decision: Perform branch/history scrubbing without credential rotation.
Reason: Chase explicitly requested the scrub now and deferred rotation; the branch should stop carrying plaintext secret material while operational credentials remain unchanged until a separate rotation task.

## 2026-05-18 20:52:32 — SIGNED_BY_AGENT
Decision: Complete the Rust bridge task without promoting Echo live ownership.
Reason: the Rust bridge passed isolated NATS transport proof, but live promotion should happen only after a real Hermes-backed native subject test and an explicit secret scrub/rotation task.

## 2026-05-18 20:51:58 — SIGNED_BY_AGENT
Decision: Verify the Rust bridge with `/bin/echo` and isolated `nova.echo.native.test` subjects before any real Hermes model turn.
Reason: this proves NATS ping, direct delivery, queueing path, and `reply_to` chunking without spending model tokens or disturbing the healthy visible Echo owner.

## 2026-05-18 20:35:08 — SIGNED_BY_AGENT
Decision: Build the Hermes/NATS replacement as a Rust sidecar first, using a test subject before taking over Echo's live direct subject.
Reason: Rust and systemd match the platform rules, but `nova.echo.direct` is currently healthy and should remain on the visible bridge until the Rust path proves reply, queue, trace, and rollback behavior.

## 2026-05-18 15:44:47 — SIGNED_BY_AGENT
Decision: Complete the Echo/NATS master task with the visible Echo bridge as active owner and native adapter documented as a spike.
Reason: live NATS reply proof passes through the visible bridge, while the native path still carries provider-auth risk before fallback and should not replace the stable owner without a separate promotion task.

## 2026-05-18 15:43:40 — SIGNED_BY_AGENT
Decision: Close the master task only after checking all child completion reports and live service/route state.
Reason: the master task is an acceptance gate, not another planning document.

## 2026-05-18 15:42:28 — SIGNED_BY_AGENT
Decision: Keep Skipper out of live NATS delegation until a dedicated Skipper owner is enabled.
Reason: live ping validation still returns no responder for `nova.skipper.ping`, so treating Skipper as reachable over NATS would create silent task loss.

## 2026-05-18 15:41:59 — SIGNED_BY_AGENT
Decision: Keep Latch as the only task-board mover and final integrator in the delegation protocol.
Reason: parallel nova work is useful only if ownership, rollback, and completion-report authority remain unambiguous.

## 2026-05-18 15:40:58 — SIGNED_BY_AGENT
Decision: Gate CryptPad or any collaborative server behind systemd packaging and auth review.
Reason: collaboration servers can create new exposed state and logs; they need the same operational discipline as agent services before becoming part of the platform.

## 2026-05-18 15:40:38 — SIGNED_BY_AGENT
Decision: Treat collaboration tools as artifact producers around NovaOps, not runtime dependencies of the control plane.
Reason: design tools should reduce ambiguity and improve QA without becoming another source of service ownership, auth, or secret exposure risk.

## 2026-05-18 15:39:44 — SIGNED_BY_AGENT
Decision: Make deterministic redaction a first implementation slice for analytics/logs.
Reason: useful observability requires correlating repeated events, but raw credentials and connection strings must never be rendered in the UI or reports.

## 2026-05-18 15:39:24 — SIGNED_BY_AGENT
Decision: Build analytics/logs around live NATS and systemd sources instead of cloned dashboard metrics.
Reason: the platform's truth comes from bus traces, journal units, service ownership, and bridge reply capture rather than a separate web UI session database.

## 2026-05-18 15:38:34 — SIGNED_BY_AGENT
Decision: Treat `reply_captured` as the authoritative UI boundary for assistant content.
Reason: earlier Echo work proved delivery can succeed before actual assistant-row extraction, so the console must distinguish transport progress from captured answer text.

## 2026-05-18 15:38:14 — SIGNED_BY_AGENT
Decision: Specify the chat/session console before implementation.
Reason: the UI needs stable data contracts for NATS delivery, reply capture, and visible CLI handoff before code work begins.

## 2026-05-18 15:37:27 — SIGNED_BY_AGENT
Decision: Base the first NovaOps UI specs on a calm three-panel console plus fleet-specific NATS/service state.
Reason: this preserves the strongest user-facing idea from the Hermes UI candidates while grounding the platform in the actual Echo/NATS/systemd runtime.

## 2026-05-18 15:36:11 — SIGNED_BY_AGENT
Decision: Convert UI extraction into three implementation-spec tasks rather than immediate frontend code.
Reason: the current goal is to make the borrowed ideas actionable and safe for later implementation while preserving Chase's no-straight-pull boundary.

## 2026-05-18 15:32:59 — SIGNED_BY_AGENT
Decision: Extract UI patterns and implementation tasks, not code, from Hermes Web UI candidates.
Reason: Chase explicitly asked to rebuild/adapt ideas rather than straight-pull another project's implementation.

## 2026-05-18 15:31:27 — SIGNED_BY_AGENT
Decision: Paperclip receives a documentation adapter package for the Nova fleet before any direct runtime integration.
Reason: stable docs/config inventory gives Skipper and future Paperclip agents a safe operating map without granting runtime mutation authority or exposing secrets.

## 2026-05-18 15:30:46 — SIGNED_BY_AGENT
Decision: Sync Paperclip with redacted operational inventory instead of copying raw profile config/auth files.
Reason: Paperclip needs usable fleet context, but profile configs and auth stores may contain live tokens or provider credentials.

## 2026-05-18 15:30:03 — SIGNED_BY_AGENT
Decision: Leave `gateway.py` unchanged for the direct Echo phone path.
Reason: the gateway already forwards the NATS `reply_to` stream to SSE, and the corrected Echo bridge now supplies actual assistant text instead of a delivery acknowledgement.

## 2026-05-18 15:28:22 — SIGNED_BY_AGENT
Decision: Verify the phone route against the serialized/reply-capturing Echo visible bridge before changing gateway code.
Reason: Echo NATS now returns real answer text, so the phone path may already satisfy the truthful-response requirement if it forwards `reply_to` output correctly.

## 2026-05-18 15:27:05 — SIGNED_BY_AGENT
Decision: Defer Skipper NATS enablement instead of adding Skipper to the live bridge roster during validation.
Reason: Skipper has profile/config/active-directory state but no current subject owner; enabling it changes project routing semantics and should be an explicit delegation decision, not a hidden validation side effect.

## 2026-05-18 15:26:19 — SIGNED_BY_AGENT
Decision: Disable `pipecat-roster-agents.service` as a low-risk duplicate-owner fix.
Reason: the fallback roster bridge overlaps with the profile-backed Hermes bridge and Echo visible bridge; keeping it active violates the one-owner subject rule and can produce stale or duplicate answers.

## 2026-05-18 15:24:58 — SIGNED_BY_AGENT
Decision: Build a read-only fleet matrix first and defer dangerous service ownership changes into new task folders.
Reason: multiple Hermes gateways are already running under systemd, and the acceptance criteria require one clear owner per subject without disrupting unrelated profiles.

## 2026-05-18 15:23:53 — SIGNED_BY_AGENT
Decision: Keep the visible Echo TUI bridge as active rollback owner after proving the native Hermes NATS adapter.
Reason: the native adapter works and returns real NATS replies without X11, but Echo's gateway provider path currently emits OpenAI/Codex `HTTP 401` before fallback, so making native ownership permanent would trade a stable visible path for a credential-risky default.

## 2026-05-18 15:23:53 — SIGNED_BY_AGENT
Decision: Use Hermes' plugin platform path for NATS instead of hard-coding `gateway/platforms/nats.py`.
Reason: Hermes' own `ADDING_A_PLATFORM.md` recommends plugins for third-party/community adapters, and the plugin path gives native gateway behavior without editing core factory/config files.

## 2026-05-18 15:14:17 — SIGNED_BY_AGENT
Decision: Treat the native Hermes NATS adapter as a spike with the temporary visible bridge kept as rollback.
Reason: the visible bridge is now serialized and returns real answer text, so native adapter work can proceed without risking the current operator-visible path.

## 2026-05-18 15:13:33 — SIGNED_BY_AGENT
Decision: Flatten Echo visible bridge prompts before typing and capture replies from the Hermes SQLite message table.
Reason: multiline desktop typing can split the envelope into multiple TUI user rows; a compact single-line envelope preserves the event id and message in one stored prompt, making assistant-row extraction deterministic.

## 2026-05-18 15:10:25 — SIGNED_BY_AGENT
Decision: Implement reply capture after serialization using Echo's local Hermes CLI session database.
Reason: the visible TUI bridge already proves delivery and session advancement; the missing contract is returning the assistant row that Echo actually wrote for the NATS-triggered turn.

## 2026-05-18 15:09:35 — SIGNED_BY_AGENT
Decision: Use an internal bounded FIFO worker for Echo visible NATS delivery instead of relying on the NATS callback body as the serialization boundary.
Reason: callback-level blocking can hide queued messages and makes queue telemetry ambiguous; a dedicated worker separates NATS receive, queue accounting, visible typing, and completion/timeout reporting.

## 2026-05-18 15:03:58 — SIGNED_BY_AGENT
Decision: Serialize Echo visible bridge delivery before adding answer extraction.
Reason: reply capture is not reliable until rapid inbound NATS messages cannot interrupt the active visible model turn.

## 2026-05-18 15:05:12 — SIGNED_BY_AGENT
Decision: Treat `echo-tui-nats-bridge.service` as the current Echo subject owner and keep `pipecat-hermes-agents.service` responsible only for non-Echo profile-backed agents.
Reason: Live service files and logs show Echo is omitted from `HERMES_AGENT_NAMES`, while Echo ping returns `pong:echo:tui` and the visible CLI proof advanced Echo's CLI session.

## 2026-05-18 15:01:18 — SIGNED_BY_AGENT
Decision: Lock the current Echo visible NATS state before editing bridge code.
Reason: `01-lock-current-state` is the baseline gate for later serialization, reply capture, fleet sync, Paperclip sync, and UI extraction work.

## 2026-05-18 14:27:18 — SIGNED_BY_AGENT
Decision: Decompose the Echo/NATS plan into filesystem task folders instead of relying on a single plan document.
Reason: Chase wants `/goal` to start from a concrete file path and expects the operator workflow `to_do -> in_progress -> completed` with `completion_report.md` per task. Separate task folders also make it safe to delegate Paperclip sync to Skipper and architecture/QA lanes to Vaeris, Tecton, Echo, and Iris.

## 2026-05-18 14:02:12 — SIGNED_BY_AGENT
Decision: Quote frontmatter descriptions that contain colons.
Reason: unquoted YAML scalar values cannot safely contain `: `, and the skill loader rejects invalid frontmatter before the skill body can be used.

## 2026-05-18 13:58:43 — SIGNED_BY_AGENT
Decision: Store the new Hermes/NATS operating procedures as skills in `skills_master` instead of only adding another plan note.
Reason: Chase needs the next session to recover operational muscle memory quickly after sleep; skills are the reusable surface for opening/commanding agents and using NATS without re-deriving the exact commands.

## 2026-05-18 13:54:08 — SIGNED_BY_AGENT
Decision: Put Echo's fresh OpenAI/Codex access token in `model.api_key` and also update Echo's `auth.json` provider/pool state.
Reason: Hermes' `openai-codex` provider normally resolves OAuth from Echo's profile auth store, but the operator explicitly required the key to be in Echo's config; setting `model.api_key` makes the runtime use the fresh token directly, while synchronizing `auth.json` prevents the stale revoked pool entry from being selected on paths that still consult the auth store.

## 2026-05-18 13:42:22 — SIGNED_BY_AGENT
Decision: Treat the visible terminal evidence as authoritative over Echo's self-report for provider status.
Reason: Echo answered that no OpenAI token error appeared, but the terminal output showed `HTTP 401 token_revoked` followed by fallback to NVIDIA `minimax-m2.7`; NATS delivery works, while the OpenAI/Codex auth token remains stale or revoked.

## 2026-05-18 13:25:43 — SIGNED_BY_AGENT
Decision: Relaunch Echo through the visible CLI path and keep the temporary NATS-to-TUI bridge as the immediate inspection path.
Reason: Chase needed Echo reopened after switching accounts, and the next proof needed to hit the operator-visible session rather than only NATS monitor output. Echo's answer inferred a stale pipecat-voice cwd, so the process cwd was verified directly from `/proc/1991425/cwd`.

## 2026-05-18 12:31:28 — SIGNED_BY_AGENT
Decision: Move Echo off the background Hermes subprocess bridge and onto a dedicated visible TUI NATS bridge.
Reason: background `hermes chat -q --continue` turns update session state but do not appear in the operator-visible Echo CLI. Chase explicitly needs NATS messages to hit Echo's visible session.

Decision: Treat the visible TUI bridge as temporary and document the native NATS platform adapter as the target.
Reason: X11 input proves visible delivery quickly, but production Echo-on-NATS needs actual reply capture, `reply_to` response publishing, and platform-owned session semantics.

Decision: Stop the five-long-turn test after OpenAI/Codex returned HTTP 429.
Reason: the NATS-to-visible-CLI path is delivering, but Echo cannot produce long answers while the selected provider is rate-limited.

## 2026-05-18 11:22:07 — SIGNED_BY_AGENT
Decision: Make Echo NATS traffic use the canonical Hermes CLI `--continue` path instead of Echo's API server path.
Reason: the API server created a separate `api_server` session while the operator was working in Echo's visible CLI lineage. Removing Echo-specific bridge overrides lets NATS use Echo's current profile config (`gpt-5.4-mini`) and resume the latest CLI session.

Decision: Leave Echo's standalone API gateway process running but stop using it for NATS bridge turns.
Reason: the local API endpoint may still be useful for direct API clients, but NATS voice routing should not silently fork Echo into a separate API-session reality.

## 2026-05-18 11:17:15 — SIGNED_BY_AGENT
Decision: Send Echo's operator note through the visible CLI instead of the NATS/API bridge.
Reason: the operator wanted the message to appear in Echo's terminal session; the NATS/API bridge routes to the gateway session and is not visible in the TUI.

## 2026-05-18 11:00:47 — SIGNED_BY_AGENT
Decision: Clear the visible TUI input buffer before each automated desktop send.
Reason: Hermes TUI can preserve partial command text across focus changes; clearing first prevents automated input from being concatenated onto stale slash commands.

## 2026-05-18 10:57:03 — SIGNED_BY_AGENT
Decision: Retry visible terminal input after the first prompt initialized Echo's TUI.
Reason: the operator identified first-prompt initialization as the reason the previous desktop input did not visibly behave as expected.

## 2026-05-18 10:55:49 — SIGNED_BY_AGENT
Decision: Use desktop-level input for the visible Echo CLI after TTY injection failed.
Reason: the host blocks `TIOCSTI`, so the reliable way to place text in the operator-visible GNOME Terminal is to activate the window and type through X11.

## 2026-05-18 10:54:06 — SIGNED_BY_AGENT
Decision: Send the operator's Echo note as a structured `nova.echo.direct` bridge message with a reply inbox.
Reason: the structured envelope verifies delivery through the same path used by voice/NATS routing and provides explicit receipt confirmation.

## 2026-05-18 10:50:23 — SIGNED_BY_AGENT
Decision: Run Echo's API gateway with the global `--yolo` flag as well as the visible CLI.
Reason: bridge/API turns should match the operator's approval posture and avoid approval stalls while Echo is being driven through NATS and the local API server.

## 2026-05-18 10:49:26 — SIGNED_BY_AGENT
Decision: Verify Echo with a structured NATS bridge request instead of plain `nats req`.
Reason: the bridge expects a JSON envelope with `message` and `reply_to`; plain request text does not exercise the live Hermes bridge handler correctly.

## 2026-05-18 10:47:41 — SIGNED_BY_AGENT
Decision: Restart only Echo's Hermes API gateway and reopen the visible Echo CLI after the profile was changed to direct Haiku 4.5.
Reason: Echo's profile configuration is the source of truth for the API server route, and reopening the terminal from `/adapt/novas/active/echo` preserves the operator-required working directory without disrupting the rest of the nova gateway fleet.

## 2026-05-18 09:12:25 — SIGNED_BY_AGENT
Decision: Validate Echo through NATS rather than injecting text into the visible CLI.
Reason: pipecat voice traffic uses `nova.echo.direct`; testing that route verifies the bridge path without disturbing the operator's open Echo terminal session.

## 2026-05-18 09:05:46 — SIGNED_BY_AGENT
Decision: Run bridge-spawned Hermes turns from the agent's active nova directory instead of the pipecat control-plane directory.
Reason: operator direction requires agent work to occur under `/adapt/novas/active/<name>`; pipecat is only the transport/control plane and should not become the agent's runtime cwd.

Decision: Do not force-create `/adapt/novas/active/agent-5`.
Reason: `agent-5` has a Hermes profile but no active nova directory. Falling back to `/home/x/.hermes/profiles/agent-5` is safer than inventing a nova active directory without an explicit owner decision.

## 2026-05-18 08:57:44 — SIGNED_BY_AGENT
Decision: Test `agent-5` directly through Hermes CLI before adding it to NATS bridge routing.
Reason: `agent-5` is not currently part of the voice bridge roster, so a direct bounded turn validates the profile/model path without changing live NATS subscriptions.

## 2026-05-18 08:52:05 — SIGNED_BY_AGENT
Decision: Open Echo in a real GNOME Terminal CLI rather than a detached service/session wrapper.
Reason: the operator asked for an open CLI; the host has no `tmux`, but GNOME Terminal is available on the active desktop display and gives Echo a visible interactive terminal.

Decision: Leave Deepgram voice transport unchanged while preparing to move the model side to Grok.
Reason: Deepgram currently provides the voice transport path; moving model inference to Grok is an upcoming provider routing change once subscriptions and credentials are available.

## 2026-05-18 08:47:20 — SIGNED_BY_AGENT
Decision: Honor Echo's profile config for bridge turns and raise the end-to-end timeout window to 300 seconds.
Reason: the operator identified the low retry/timeout policy as the immediate cause of Echo bridge cutoffs and closed the stale large CLI session. The gateway timeout must match the bridge timeout or the caller still disconnects early.

Decision: Keep Echo in fresh bridge-session mode for now.
Reason: the stale large Echo CLI lineage is closed, but fresh mode avoids accidentally reattaching to an oversized historical session while the NIM provider path is still slow.

## 2026-05-18 08:22:14 — SIGNED_BY_AGENT
Decision: Run Echo bridge turns as fresh lean MiniMax invocations instead of resuming Echo's heavy CLI lineage.
Reason: Echo's latest session had 433 messages and a 580 KB message payload, causing timeouts. A clean named session worked once but became variable on resume. Fresh MiniMax bridge turns with profile config/rules ignored returned through NATS in 15.3 seconds, which is operationally useful for voice.

Decision: Add per-agent override hooks to `hermes_nats_agents.py`.
Reason: Echo needs a different session/model/isolation profile than the rest of the fleet; global bridge settings would either slow Echo or disturb other agents that are already synced.

## 2026-05-18 07:50:17 — SIGNED_BY_AGENT
Decision: Make `hermes-nats-agents` the single owner for local Hermes profile-backed NATS subjects, and leave `roster-agents` only as a non-profile fallback.
Reason: NATS showed overlapping `hermes-nats-agents` and `roster-agents` subscriptions on the same nova subjects, which can produce duplicate or stale replies. The user asked for agent/CLI-session sync, so the real Hermes profile path must win.

Decision: Resume the latest existing CLI session with bare `--continue` instead of using a generated named session.
Reason: Hermes rejects `--continue <name>` when no session with that name already exists. Bare `--continue` attaches to the profile's existing session lineage, which matches the current operator workflow where several profiles already have active CLI sessions.

## 2026-05-18 02:55:00 — SIGNED_BY_AGENT
Decision: Add a one-time client-side reload after service-worker/cache cleanup.
Reason: Playwright proved the page rendered the new orange theme but one stale
Cache API bucket could survive initial cleanup in the same browser session. A
single post-cleanup reload forces the document, CSS, and scripts onto network
assets after cache deletion.

## 2026-05-18 02:48:00 — SIGNED_BY_AGENT
Decision: Temporarily disable persistent PWA service-worker caching and force
cache deletion/unregistration from the live client.
Reason: Playwright validated that the public network page had the new orange
theme, so continued user reports of no change pointed to stale installed app or
browser service-worker state. Removing the service worker is the fastest way to
force the UI to reflect current network assets.

## 2026-05-18 02:41:12 — SIGNED_BY_AGENT
Decision: Change the service worker from cache-first static handling to
network-first for all same-origin GET requests.
Reason: the user still saw no color change because the installed/browser
session could keep serving cached static assets under the same versioned URL.
Network-first plus `orange5` ensures the browser sees the corrected pure-black
and dark-orange theme.

## 2026-05-18 02:18:05 — SIGNED_BY_AGENT
Decision: Drop copper from the visible Blackline primary palette and use pure
black background with dark orange/amber/gold/silver accents.
Reason: the copper pass still read as brown in the rendered UI. The user's
direction requires pure black and dark orange, so the background was flattened
to `#000000` and the brown-leaning accent was removed from the primary path.

## 2026-05-18 02:06:10 — SIGNED_BY_AGENT
Decision: Remove custom install controls entirely while keeping service-worker
registration active.
Reason: the user explicitly asked to remove the install button. Keeping the
service worker preserves PWA/offline behavior without presenting a custom
install CTA in the UI.

Decision: Version the Blackline CSS/JS URLs and bump the service-worker cache
after strengthening the palette.
Reason: installed/mobile clients can continue showing stale cached assets even
after a service restart. Versioned URLs plus a new cache key make the color
change visible without requiring users to manually clear browser storage.

## 2026-05-18 01:31:40 — SIGNED_BY_AGENT
Decision: Force-terminate the old `pipecat-voice.service` process through
`systemctl kill --signal=SIGKILL` after a normal restart hung in
`stop-sigterm`.
Reason: systemd was unable to complete the restart naturally, leaving the old
gateway process active and preventing the new `/sw.js` route from being served.
Using systemd's kill path preserved unit supervision and allowed a clean start
with the updated gateway code.

## 2026-05-18 00:00:57 — SIGNED_BY_AGENT
Decision: Replace the initial brown/tan leaning Blackline palette with a metal
stack centered on copper and amber, supported by dark orange for danger/action,
gold for premium highlights, and silver for neutral/live contrast.
Reason: preserves the user's pure-black/no-purple/no-teal/no-magenta direction
while making the UI feel sharper, more premium, and less sepia.

## 2026-05-17 08:20:58 — SIGNED_BY_AGENT
Decision: Consolidate the repo's primary web experience into exactly three core
PWA products on existing routes instead of adding more standalone pages.
Reason: the repo already had overlapping dashboard, monitor, canvas, kanban,
and whiteboard surfaces. Three installable shells on `/`, `/dashboard`, and
`/canvas` cover the same operational domain with less fragmentation while still
reusing the current backend endpoints and websocket contracts.

Decision: Use pure black with warm metallic accents for the new shells.
Reason: satisfies the explicit no-purple / no-teal / no-magenta constraint,
preserves strong contrast in low-light mobile usage, and creates a consistent
visual identity across the three apps without leaning on the previous mixed
palette.

## 2026-05-16 06:56:00 — SIGNED_BY_AGENT
Decision: Implement voice→nova as a Hermes platform-adapter plugin inside the
warm `hermes gateway` runtime, not the cold-subprocess bridge or a Haiku
shortcut.
Reason: gives the real agent (own LLM, session, tools, memory) with sub-second
latency and no identity fork; the plugin path requires zero Hermes core
changes. Adapter conforms to the pre-existing fleet schema
(extra.url/subjects[]/queue_group) already staged in herald/iris configs —
that schema was the intended-but-unimplemented contract; we built the missing
half rather than inventing a new one.

Decision: Pilot on TECTON only; herald/iris `platforms.nats.enabled` set to
false.
Reason: their nats config was a dormant no-op (no adapter existed). With the
adapter now discoverable, an auto-restart would have bound them with a path
that double-answers against the cold bridge. Disabling preserves current
behavior and isolates the pilot.

Decision: Surface tecton memory overflow (67442/22000) as a blocker rather
than auto-remediating.
Reason: trimming/clearing a nova's memory is high-blast-radius and is tecton
profile data, not plugin scope. Continuity cannot be validated until tecton's
memory backend is within cap; flagged for owner decision before merging the
feature branches.

## 2026-05-10 20:30:00 — SIGNED_BY_AGENT
Decision: Use Haiku (claude-haiku-4-5-20251001) as umbrella LLM for vox peer, preserving NATS
for all other nova agents.
Reason: Headless Claude Code (vox-agent.service) incurs 18-20s cold-start latency and delivers
non-streaming responses. Haiku via Anthropic streaming API achieves ~1-2s TTFT with token-level
streaming. For the default voice persona (vox), this eliminates the most user-perceptible delay.
Specialized agents (herald, tecton, iris, etc.) retain NATS routing unchanged — they have richer
context needs that justify the latency tradeoff.

Decision: Set endpointing=800ms in Deepgram listen provider.
Reason: Default Deepgram VAD endpointing was cutting Chase's turns mid-sentence at natural speech
pauses. 800ms gives enough silence headroom for typical intra-sentence pauses while keeping
turn-taking responsive. Configurable at the SettingsConfiguration level; adjustable per session
if needed.

## 2026-05-10 19:44:00 — SIGNED_BY_AGENT
Decision: Proxy Deepgram WS through gateway.py instead of connecting browser directly.
Reason: `/v1/auth/grant` tokens are scoped to `asr:write` only; the Voice Agent API
requires a full API key or a token with broader scope. Proxying keeps the key server-side,
eliminates the 30s token TTL problem, and gives us full visibility into the DG protocol for
future debugging.

## 2026-05-10 19:17:00 — SIGNED_BY_AGENT
Decision: Replace WebRTC+pipecat with Deepgram Voice Agent API.
Reason: WebRTC state is process-local (ICE, DTLS); any service restart drops the user mid-session.
Deepgram Voice Agent API handles STT/VAD/TTS server-side over a single browser WebSocket, eliminating
the pipecat pipeline and WebRTC entirely. No process-local media state means future gateway.py
restarts do not terminate voice sessions (browser reconnects to Deepgram directly).

Decision: Deepgram "think" endpoint = our /v1/chat/completions gateway (OpenAI SSE format).
Reason: Deepgram's agent calls any OpenAI-compatible completions endpoint. Our gateway extracts
the last user message, publishes to NATS, and streams chunks as SSE. This preserves the full
nova NATS routing layer unchanged — peer/channel selection via query params.

Decision: Live agent routing via SettingsConfiguration resend (not WS reconnect).
Reason: Deepgram accepts a new SettingsConfiguration at any time on the open WS connection, updating
the think endpoint URL in-flight. This means selectAgent() changes the NATS target with zero latency
and no audio interruption.

Decision: PCM capture via ScriptProcessor (not AudioWorklet).
Reason: iOS Safari support for AudioWorklet is incomplete as of 2026. ScriptProcessor is deprecated
but universally supported including iOS PWA. Nearest-neighbour downsample from actual sampleRate
(commonly 44.1kHz or 48kHz on iOS despite the 16kHz hint) to 16kHz is sufficient for speech.

Decision: 24kHz playback AudioContext shared across all TTS chunks (gapless chaining).
Reason: Creating a new AudioContext per TTS chunk causes gaps and "click" artifacts. A single
persistent context with `nextPlayTime` scheduling chains buffers without gaps.

## 2026-05-10 01:07:00 — SIGNED_BY_AGENT
Decision: Debounce TranscriptionFrames in NATSAgentProcessor (600ms) rather than fixing at STT
or VAD level.
Reason: Deepgram's endpointing runs independently of SileroVAD — Deepgram sends `is_final=True`
results at sentence boundaries regardless of Pipecat's VAD stop_secs setting. Adjusting Deepgram
endpointing params would require API-level configuration and may affect transcription quality.
The NATS bridge is the correct chokepoint: buffer here, join, publish once per conversational turn.
600ms chosen as safe window above Deepgram's typical inter-sentence gap (~200-400ms).

## 2026-05-09 10:41:39 — SIGNED_BY_AGENT
Decision: Drop `--resume` entirely; use fresh `claude --print -p "..."` for fallback turns.
Reason: `--resume` of a busy interactive session exits rc=1 immediately (CC enforces a
"session busy" lock). Fresh sessions have no such constraint and the nats-channel MCP
is available via user-scope settings.json, so no `--dangerously-load-development-channels`.

Decision: Register nats-channel MCP in `~/.claude/settings.json` (user-scope), not per-project.
Reason: fallback CC is spawned in the pipecat repo CWD which has no project-level MCP config;
user-scope registration makes the tool available in every CC session unconditionally.

Decision: Add `/home/x/.cache` to `ReadWritePaths` in vox-agent.service.
Reason: CC writes MCP log files to `/home/x/.cache/claude-cli-nodejs/<cwd>/mcp-logs-*/`
during initialization. Under `ProtectSystem=strict` this path was read-only, causing rc=1
with no output. Confirmed via strace EROFS errors. The directory already existed and is
the correct CC cache location; exposing it is the minimal viable fix.

## 2026-05-07 23:27:00 — SIGNED_BY_AGENT
Decision: Use `claude --print --resume <session-id> --input-format=stream-json` as the
headless turn-trigger mechanism, not tmux.
Reason: tmux is not present in this environment; `--print --resume` is the only
supported non-interactive prompt-injection path in CC. `--remote-control` uses Anthropic
cloud bridge (requires backend connectivity, not a local IPC bus). Session ID is
discoverable from `~/.claude/sessions/*.json` by matching `name` field.

Decision: Watch `reply_to` inbox (not `nova.chase.direct`) to detect whether interactive
CC already replied.
Reason: `nats_reply` tool in nats_channel_mcp.py publishes to the `_INBOX.*` address
in `reply_to`, not to any fixed nova subject. Old debounce was subscribing to the wrong
subject and never firing.

Decision: Set default VOX_CLAUDE_TIMEOUT_S=120s.
Reason: Headless CC must run ToolSearch to fetch deferred nats_reply schema before
calling the tool. In testing, tool invocation occurred at ~60-90s from subprocess start.
45s was too short and caused pre-delivery kill.

## 2026-05-07 02:46:35 — SIGNED_BY_AGENT
Domain: `adapted.ai` not in this CF account; user redirected to `adaptdev.ai`.
Used existing `vertex-ai` tunnel (one tunnel, multiple zones) instead of
creating a new tunnel. Reason: faster, no new credentials, same TLS posture.

## 2026-05-07 02:46:35 — SIGNED_BY_AGENT
LLM stage replaced by `NATSAgentProcessor`. Agent identity = the user (chase).
Default peer = `echo`, overridable per session via `?to=<name>` URL param.
Streaming contract: per-request `reply_to` inbox carrying chunked JSON, with
legacy fallback for non-streaming agents that publish back on
`adapt.<self>.<channel>`.

## 2026-05-20 09:17:00 — crew visible terminal orchestration
Skipper took lead after Latch quota-max. Launched all 5 crew (Iris, Zap, Forge, Synergy, Tecton) in separate visible GNOME Terminal windows. Pattern: `DISPLAY=:0 gnome-terminal --title='<Name> CLI' --working-directory=/adapt/novas/active/<name> -- bash -lc 'exec hermes -p <name> --yolo -c'`. Verified via pgrep, xdotool window search, and readlink /proc/<pid>/cwd. Task files staged at ops/crew-comms/inbound/. Dispatch via xdotool type to each visible window.
Task 30 promotion: no-go on replacing TUI bridges. Adapter proves isolated behavior but live CLI turn execution stays with echo-tui-nats-bridge.service and skipper-tui-nats-bridge.service pending explicit future promotion.

## 2026-05-20 09:10:00 — NATS Comms Training: Echo confirms, Tecton pending
NATS delivery into visible Echo CLI confirmed at nova.skipper.direct. Echo executed the lesson: sent 1-on-1 to tecton, looped broadcast to crew, reported envelope schema {id, from, message, reply_to}. Tecton window live but no inbound confirmation yet.
