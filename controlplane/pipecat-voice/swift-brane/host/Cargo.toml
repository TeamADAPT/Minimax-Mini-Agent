[package]
name = "swift-brane-host"
version = "0.1.0"
edition = "2021"
description = "Swift Brane host shim — invokes wasm64-native tools with zero Python subprocess"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
